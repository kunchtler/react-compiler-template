# Vite React Template

This template for vite provides support for
- the React compiler
- ESLint, with additional plugins
- Prettier

It was primarily made for my own use, so it contains my own preferences (see eslint.config.ts, and package.json for the custom commands and prettier rules), but published in hope it would be useful to somebody else. 